on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build, Push and Deploy to Heroku
        id: heroku
        uses: snithyanantham/docker-compose-multiple-apps-heroku-deploy@v1.0
        with:
          email: "bmarin@csumb.edu"
          api_key: "55bd9413-179d-48a2-8a12-d250bfb3983e"
          docker_compose_file: './docker-compose.yml'
          heroku_apps: '[{"imagename":"wishlist_backend","appname":"app_backend","apptype":"web"},{"imagename":"wishlist_frontend","appname":"app_frontend","apptype":"web"}]'

